import { getEvenNumbers, getSumNumbersBiggerThan } from './sumAndEvenFilter'
import { myFilter, myMap, filterMap } from './AlmightyReduce'

const arrays = {
  getEvenNumbers,
  getSumNumbersBiggerThan,
  myFilter,
  myMap,
  filterMap,
}

export default arrays
